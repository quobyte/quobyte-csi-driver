apiVersion: v1
kind: Secret
metadata:
  name: quobyte-admin-credentials
type: "kubernetes.io/quobyte"
data:
  # base64 of the QUOBYTE_API_USER/QUOBYTE_API_PASSWORD in this directory's "env" file
  # (echo -n "value" | base64). Applied only to run the upstream Kubernetes e2e
  # (external-storage) suite after this directory's Go test passes -- see run_test.
  user: YWRtaW4=
  password: cXVvYnl0ZQ==
